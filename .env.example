# Supabase Configuration (Required)
# Get these from your Supabase project settings: https://app.supabase.com
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-key-here

# Ollama Configuration
# For Docker Compose: use http://ollama:11434
# For local development with Ollama installed: use http://localhost:11434
OLLAMA_BASE_URL=http://ollama:11434

# Model Configuration
# Embedding model from HuggingFace (1024-dimensional vectors)
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B
# LLM model available in Ollama
LLM_MODEL=llama3.2:latest
# Reranker model from HuggingFace
RERANKER_MODEL=Qwen/Qwen3-Reranker-0.6B

# Reranking Configuration
# Enable/disable reranking (true/false)
RERANKER_ENABLED=true
# Number of documents to retrieve before reranking
INITIAL_RETRIEVAL_COUNT=20
# Number of documents to keep after reranking
FINAL_RESULT_COUNT=5

# Vector Database Settings
VECTOR_TABLE_NAME=it_docs_embeddings
# Minimum similarity score (0-1) for document matching
MATCH_THRESHOLD=0.7
# Number of similar documents to retrieve (used when reranker is disabled)
MATCH_COUNT=3

# Flask Configuration
FLASK_ENV=development
PORT=5000